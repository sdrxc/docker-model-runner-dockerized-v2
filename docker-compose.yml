services:
  app:
    build: ./app
    env_file: ./backend.env
    ports:
      - "8501:8501"
    depends_on:
      - llm
  llm:
    provider:
      type: model
      options:
        model: ${LLM_MODEL_NAME:-ai/smollm2:latest}